apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: gatemaster-app
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: gatemaster-app
  policy:
    numerical:
      order: asc